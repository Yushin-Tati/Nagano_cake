<h1>ジャンル編集</h1>
<%= form_with model: [:admins, @genre], local:true do |f| %>
  <teble>
    <tr>
      <th></th>
      <th></th>
      <th></th>
    </tr>
    <tr>
      <td>ジャンル名</td>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit '変更を保存' %></td>
    </tr>
  </teble>
<% end %>