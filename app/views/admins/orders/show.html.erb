<h1>注文履歴一覧</h1>

<table>
  <tr>
    <th>購入日時</th>
    <th>購入者</th>
    <th>注文個数</th>
    <th>注文ステータス</th>
  </tr>
  <% @orders.each do |order| %>
    <tr>
      <td><%= order.create_at %></td>
      <td><%#= @customer.last_name(id: order.customer_id) @customer.first_name(id: order.customer_id) %></td>
      <td><%#= @order_detail.amount(id: order.id) %></td>
      <td><%= order.status %></td>
    </tr>
  <% end %>
</table>